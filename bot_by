import telebot
from telebot import types
import os

TOKEN = os.getenv("BOT_TOKEN")
bot = telebot.TeleBot(TOKEN)

def main_menu():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.row('Basic Science', 'Clinical Science')
    return markup

def basic_science_menu():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.row('Terminology', 'Anatomy')
    markup.row('Biochemistry', 'Physiology')
    markup.row('Pathology', 'Pharmacology', 'Microbiology')
    markup.row('🔙 Back', '🔝 Main Menu')
    return markup

def anatomy_menu():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.row('Gross Anatomy', 'Histology', 'Embryology')
    markup.row('🔙 Back', '🔝 Main Menu')
    return markup

def clinical_science_menu():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    markup.row('Internal Medicine', 'Obs/Gyn')
    markup.row('Surgery', 'Pediatrics')
    markup.row('Community Medicine', 'Forensic Medicine')
    markup.row('🔙 Back', '🔝 Main Menu')
    return markup

@bot.message_handler(commands=['start'])
def send_welcome(message):
    bot.send_message(message.chat.id,
                     "مرحباً بك في Medical Bot! اختر القائمة:",
                     reply_markup=main_menu())

@bot.message_handler(func=lambda message: True)
def handle_message(message):
    text = message.text

    if text == 'Basic Science':
        bot.send_message(message.chat.id, "اختر مادة:", reply_markup=basic_science_menu())
    elif text == 'Clinical Science':
        bot.send_message(message.chat.id, "اختر مادة:", reply_markup=clinical_science_menu())
    
    elif text == 'Terminology':
        bot.send_message(message.chat.id, "⚡ Terminology", reply_markup=basic_science_menu())
    elif text == 'Anatomy':
        bot.send_message(message.chat.id, "اختر قسم Anatomy:", reply_markup=anatomy_menu())
    elif text == 'Biochemistry':
        bot.send_message(message.chat.id, "⚡ Biochemistry", reply_markup=basic_science_menu())
    elif text == 'Physiology':
        bot.send_message(message.chat.id, "⚡ Physiology", reply_markup=basic_science_menu())
    elif text == 'Pathology':
        bot.send_message(message.chat.id, "⚡ Pathology", reply_markup=basic_science_menu())
    elif text == 'Pharmacology':
        bot.send_message(message.chat.id, "⚡ Pharmacology", reply_markup=basic_science_menu())
    elif text == 'Microbiology':
        bot.send_message(message.chat.id, "⚡ Microbiology", reply_markup=basic_science_menu())

    elif text in ['Gross Anatomy', 'Histology', 'Embryology']:
        bot.send_message(message.chat.id, f"⚡ {text}", reply_markup=anatomy_menu())

    elif text in ['Internal Medicine', 'Obs/Gyn', 'Surgery', 'Pediatrics', 'Community Medicine', 'Forensic Medicine']:
        bot.send_message(message.chat.id, f"⚡ {text}", reply_markup=clinical_science_menu())

    elif text == '🔙 Back':
        bot.send_message(message.chat.id, "اختر:", reply_markup=main_menu())
    elif text == '🔝 Main Menu':
        bot.send_message(message.chat.id, "ارجع للقائمة الرئيسية:", reply_markup=main_menu())
    else:
        bot.send_message(message.chat.id, "❌ خيار غير معروف، الرجاء اختيار من القوائم.", reply_markup=main_menu())

bot.infinity_polling()